<!DOCTYPE html>
<html>
  <head>
    <title>task-manager-app</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://use.fontawesome.com/releases/v5.8.2/css/all.css" rel="stylesheet">
    <meta name="viewport" content="width=device-width,initial-scale=1">
  </head>

  <body>
    <div class="hero is-info">
      <div class="hero-head dot-border-bottom">
        <nav class="navbar is-link" role="navigation" aria-label="main navigation">
          <div class="navbar-brand">
            <a class="navbar-item" href="#">
    <!--          <img src="https://bulma.io/images/bulma-logo.png" width="112" height="28">-->
            </a>
            <div class="navbar-end">
              <div class="navbar-item">
                <div class="buttons">
                  <% if logged_in? %>
                    <%= link_to "ログアウト", logout_path, { class: "button is-light" }%>
                  <% else %>
                    <div class="button is-primary">
                      <%= link_to "ユーザーを作成する", signup_path, { class: "has-text-weight-bold has-text-white"} %>
                    </div>
                    <div class="button">
                      <%= link_to "ログイン", login_path, { class: "has-text-black"} %>
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        </nav>
      </div>
      <% flash.each do |message_type, message| %>
        <%= content_tag(:div, message, class: "is-pulled-right alert alert-#{message_type}") %>
      <% end %>
      <div class="hero-body dot-border-bottom">
        <%= yield %>
      </div>
      <div class="hero-foot">
        <div class="content has-text-centered has-text-black">
          <p>
            Footer
          </p>
        </div>
      </div>
    </div>
  </body>
</html>
