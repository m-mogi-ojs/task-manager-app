<div class="container has-text-centered">
  <% if @kanbans.any? %>
    <div>
      <%= render 'form'%>
    </div>
    <div class="kanban-container">
      <div class="columns is-mobile is-multiline is-centered">
        <% @kanbans.each_with_index do |kanban, kanbanIndex| %>
          <div class="column is-12-touch is-4-desktop">
            <div id="kanban-<%= kanbanIndex %>" class="card">
              <div class="card-content">
                <div class="" style="border-bottom: 0.04rem solid #000000;">
                  <%= kanban.name %>
                  <%= link_to kanban_path(kanban), remote: true, method: :delete do %>
                    <i class="far fa-times-circle is-pulled-right" style="margin-top: 0.25rem; margin-left: 0.25rem;"></i>
                  <% end %>
                </div>
                <% if kanban.tasks.any? %>
                  <div style="padding-top: 1rem;">タスク名</div>
                  <% kanban.tasks.each_with_index do |task, taskIndex| %>
                    <p>
                      <div>
                        <span class="task"><%= task.name %></span>
                        <input type="text" id="<%= task.id %>" class="input task-edit" style="display: none"/>
                        <% if task.complete_flg %>
                          <i id="<%= task.id %>" class="complete-flg far fa-check-square is-pulled-right" style="margin-top: 0.25rem; margin-left: 0.25rem;"></i>
                        <% else %>
                          <i id="<%= task.id %>" class="complete-flg far fa-square is-pulled-right" style="margin-top: 0.25rem; margin-left: 0.25rem;"></i>
                        <% end %>
                      </div>
                    </p>
                  <% end %>
                <% end %>
                <br>
                <div class="">
                  <%= render 'tasks/form', kanban: kanban %>
                </div>
              </div>
            </div>
          </div>
        <% end%>
      </div>
    </div>
  <% else %>
    <div class="is-center">
      まずはかんばんを追加しましょう
      <%= render 'form'%>
    </div>
  <% end %>
</div>